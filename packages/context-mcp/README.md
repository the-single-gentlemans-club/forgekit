# context-mcp

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build context-mcp` to build the library.

## Running unit tests

Run `nx test context-mcp` to execute the unit tests via [Vitest](https://vitest.dev/).
