# storybook-mcp

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build storybook-mcp` to build the library.

## Running unit tests

Run `nx test storybook-mcp` to execute the unit tests via [Vitest](https://vitest.dev/).
